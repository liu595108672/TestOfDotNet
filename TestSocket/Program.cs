using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Net.Sockets;
using System.Net;


namespace TestSocket
{
    class Program
    {
        static void Main(string[] args)
        {
            //SocketServer socketServer = new SocketServer();
            SocketClinet socketClinet = new SocketClinet();

            //while (true)
            //{
            //    try
            //    {
            //        TcpListener tcpListener = new TcpListener(new IPAddress(new byte[]{ 127, 0, 0, 1 }),10043);

            //        tcpListener.Start();

            //        tcpListener.Stop();

            //        tcpListener.Start();

            //        tcpListener.Stop();
            //    }
            //    catch (Exception)
            //    {
            //        Console.Read();
            //    }
            //}

            while (true)
            {

            }
        }
    }
}
